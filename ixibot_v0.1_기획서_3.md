# IxiBot v0.1 기획서 — 텔레그램 오류 증상 보고 수집 봇

- 문서 버전: v0.1.4 (2026-08-26) — §8 "구현 완료 기록" 추가 (v0.1 구현 중 기획서 외 추가 결정·작업 사항)
- 봇의 목적: 특정 서비스를 사용하다가 겪은 **오류 증상**을 사용자로부터 수집 (누가 / 언제 / 어떤 증상)
- 작성 목적: 최소 기능 버전(v0.1)의 범위·UX·기술 스택 정의. v0.1의 핵심 목표는 **입력 UI(이름 자동완성, 날짜/시간 선택)의 사용성 검증**. 응답 데이터의 영구 저장(DB)은 v0.2로 이연.

**확정된 결정 사항 (2026-08-26)**

- 입력 UX는 **Telegram Mini App(Web App) 폼 방식**으로 진행 — 버튼을 누르면 텔레그램 안에서 폼 화면이 열리고, 이름 자동완성·네이티브 날짜/시간 피커·200자 텍스트 영역을 한 화면에서 입력 후 제출. (기존의 "채팅 내 후보 버튼 + ▲▼ 시각 증감" 방식은 낯설다는 판단으로 폐기)
- 이름 자동완성: 하드코딩된 86명 후보 기반, 타이핑하면 실시간으로 후보가 좁혀지는 검색형 입력. **목록 밖 이름은 허용 불가.**
- 증상 시각: `YYYY-MM-DD HH:MM`(24시간제), **현재 시각 디폴트**, **미래 시각 불가**, **과거 하한 없음.**
- 증상 내용: 자유 텍스트 **최대 200자.**
- 세 항목 모두 유효해야 제출 가능 (검증 실패 시 제출 버튼 비활성).
- 같은 사람이 **여러 번 제출 가능** — 횟수 제한 없음.
- 그룹방 운영: **핀 메시지 + 딥링크 버튼 → 봇과 1:1 대화**에서 폼 진입.
- 수집 내용은 서비스 오류 증상(건강정보 아님) → 별도 수집 동의 문구 불필요.

---

## 1. 봇 기본 정보

| 항목 | 값 |
|---|---|
| 봇 이름 (name) | IxiBot |
| 사용자명 (username) | @woorrr_ixi_bot |
| 링크 | t.me/woorrr_ixi_bot |
| API 토큰 | BotFather에서 재발급한 토큰을 환경변수(`BOT_TOKEN`)로 관리. **코드·문서에 절대 기재하지 않음** |

> ⚠️ **보안 필수 조치**: 최초 발급 토큰이 대화(채팅)에 평문으로 공유된 적이 있으므로, BotFather에서 `/revoke` → 봇 목록 버튼에서 `@woorrr_ixi_bot` 탭(또는 `@woorrr_ixi_bot`라고 @ 포함 입력)으로 **토큰을 재발급**한 뒤 새 토큰만 사용한다.

**`.env` 보관 방법**

1. 프로젝트 폴더에 `.env`라는 이름의 텍스트 파일을 만들고 아래 한 줄만 적는다:
   ```
   BOT_TOKEN=재발급받은_토큰
   ```
2. `.gitignore` 파일에 `.env` 한 줄을 추가한다 → git에 커밋되지 않아 저장소에 토큰이 남지 않음.
3. 코드에서는 토큰을 직접 쓰지 않고 환경변수로 읽는다:
   ```python
   from dotenv import load_dotenv
   import os
   load_dotenv()
   BOT_TOKEN = os.environ["BOT_TOKEN"]
   ```
4. 다른 PC나 서버로 옮길 때는 `.env` 파일만 안전하게 복사. Claude Code에는 "토큰은 .env의 BOT_TOKEN에서 읽어라"라고만 지시하고 토큰 값을 대화에 붙여넣지 않는다.

---

## 2. v0.1 범위

### 포함 (In Scope)
1. Mini App 폼 페이지 (정적 HTML 1장): 이름 자동완성 + 날짜/시간 피커 + 증상 텍스트 + 제출
2. 봇: `/start` 시 폼 열기 버튼 제공, 폼 제출 데이터 수신, **서버 측 재검증**, 접수 확인 메시지
3. 폼 페이지 무료 호스팅 (GitHub Pages)
4. 제출 데이터 임시 로그(JSONL 파일) 기록 — v0.2에서 DB 저장으로 교체
5. 그룹방 핀 메시지용 딥링크 (`t.me/woorrr_ixi_bot?start=report`)

### 제외 (Out of Scope → 이후 버전)
- DB 저장 (v0.2 — Google Cloud DB 예정)
- 봇 프로세스의 클라우드 배포/상시 운영 (v0.1은 로컬 PC에서 polling 실행; 폼 페이지만 호스팅)
- 관리자 기능, 통계, 데이터 조회/수정/삭제
- 사용자 인증·접근 제어

---

## 3. 사용 시나리오 (확정)

1. 그룹방에 안내 메시지를 **고정(핀)**: "증상 보고는 아래 버튼을 눌러주세요" + 딥링크 버튼 (`https://t.me/woorrr_ixi_bot?start=report`)
2. 멤버가 버튼을 누르면 봇과의 1:1 대화가 열리고, 봇이 **[📝 증상 보고하기]** 키보드 버튼을 보여줌
3. 버튼을 누르면 텔레그램 **안에서** 폼 화면(Mini App)이 열림 — 별도 앱/브라우저 이동 없음
4. 폼 작성 → [제출] → 폼이 닫히고 봇이 접수 내용을 요약한 확인 메시지를 보냄

v0.1 검증 단계에서는 그룹방 없이 1:1 대화(`/start`)만으로 테스트해도 충분하다.

---

## 4. 상세 설계

### 4-1. Mini App 폼 화면

한 화면에 세 입력 항목과 제출 버튼을 배치한다. 텔레그램이 제공하는 테마 변수(`Telegram.WebApp.themeParams`)를 적용해 다크/라이트 모드를 자동으로 따라가게 한다.

```
┌─────────────────────────────┐
│  오류 증상 보고              │
│                             │
│  이름                        │
│  [ 김정_________________ ]  │
│    ┌───────────────────┐    │
│    │ 김정면            │    │  ← 타이핑과 동시에
│    │ 김정민            │    │     실시간으로 좁혀지는
│    │ 김정현            │    │     후보 리스트
│    └───────────────────┘    │
│                             │
│  언제 겪은 증상인가요?        │
│  [ 2026-08-26 14:35  ▾ ]   │  ← 탭하면 OS 네이티브
│                             │     날짜/시간 휠 피커
│  어떤 증상을 겪었는지         │
│  알려주세요.                 │
│  ┌───────────────────────┐  │
│  │                       │  │
│  └───────────────────────┘  │
│                   132/200   │
│                             │
│  [        제출         ]    │
└─────────────────────────────┘
```

**이름 (자동완성)**

- 86명 후보 리스트를 페이지의 JS 배열에 하드코딩
- 입력창에 타이핑하면 실시간 필터링: 전방 일치 우선, 부분 포함 차선 (예: `김정` → 김정면·김정민·김정현 / `정민` → 김정민·이정민)
- 후보를 탭해야 확정. 후보에 없는 임의 텍스트는 확정 불가(입력창 아래 "목록에서 선택해 주세요" 안내)
- 빈 입력 시 후보 리스트는 표시하지 않음 (86명 전체 노출 방지, 필요 시 스크롤 전체 보기 링크는 선택 사항)

**증상 시각**

- `<input type="datetime-local">` — 탭하면 iOS/Android 네이티브 휠 피커가 열림
- 디폴트: 폼이 열린 시점의 현재 시각 (사용자 기기 시간대 = KST 가정)
- `max` 속성을 현재 시각으로 설정 → **미래 시각 선택 불가**, 과거는 무제한
- 표시·전송 형식: `YYYY-MM-DD HH:MM` (24시간제)

**증상 내용**

- `<textarea maxlength="200">` + 실시간 글자수 카운터 (`132/200`)
- 공백만 입력은 무효 처리

**제출 (클라이언트 검증 게이팅)**

- 세 항목이 모두 유효할 때만 제출 버튼 활성화 (텔레그램 기본 MainButton 사용 권장)
- 제출 시 `Telegram.WebApp.sendData(JSON.stringify({name, symptom_time, symptom_text}))` 호출 → 폼이 닫히고 데이터가 봇에 전달됨 (4KB 제한 — 본 데이터는 수백 바이트라 여유)

### 4-2. 봇 동작

```
/start (또는 딥링크 진입)
  └─▶ 안내 메시지 + ReplyKeyboardMarkup의
      [📝 증상 보고하기] 버튼 (web_app=WebAppInfo(url=폼 주소))
        │
        ▼  (사용자가 폼 작성·제출)
      Message.web_app_data 수신
        │
        ▼  서버 측 재검증 (아래)
      ┌─ 통과 → JSONL 기록 → 확인 메시지
      │   "접수되었습니다 ✅
      │    👤 김정민 / 🕐 2026-08-26 14:35
      │    📝 (증상 요약)"
      └─ 실패 → 오류 안내 + 폼 버튼 다시 제시
```

**서버 측 재검증 (필수)**: 폼 페이지는 공개 URL이므로 클라이언트 검증만 믿지 않는다. 봇이 수신 데이터에 대해 (1) JSON 스키마, (2) 이름이 86명 리스트에 존재, (3) 시각 형식 및 미래 아님, (4) 텍스트 1~200자를 다시 검사하고, 하나라도 실패하면 기록하지 않는다.

- Mini App을 여는 `web_app` 키보드 버튼과 `sendData` 반환은 **1:1 대화에서만 동작** → §3 시나리오(딥링크 → 1:1)와 정확히 부합
- 여러 번 제출: `/start` 후 버튼은 계속 남아 있으므로 몇 번이든 다시 열어 제출 가능 (제한 없음, 확정)

### 4-3. 폼 페이지 호스팅

- 정적 HTML/CSS/JS **1개 파일** → **GitHub Pages**에 배포 (무료, HTTPS 기본 제공 — Mini App은 HTTPS 필수)
- 봇 코드와 같은 저장소에 두되, Pages는 해당 파일만 공개되도록 구성 (예: `docs/` 폴더 배포)
- 페이지 갱신 = git push만으로 완료, 봇 재시작 불필요

> **참고 — 이름 목록 노출**: GitHub Pages는 공개 URL이므로 페이지 소스의 86명 실명 리스트는 URL을 아는 사람이면 볼 수 있다. URL을 외부에 공유하지 않고 검색 엔진 색인 차단(`noindex`)을 넣는 것으로 v0.1에서는 충분하다고 판단. 더 엄격히 하려면 v0.2에서 이름 리스트를 백엔드에서 `initData` 검증 후 내려주는 방식으로 전환 가능 (§7).

---

## 5. 기술 스택

| 구분 | 선택 | 근거 |
|---|---|---|
| 봇 언어 | **Python 3.11+** | 텔레그램 봇 생태계 성숙, Claude Code로 작성·수정 용이 |
| 봇 프레임워크 | **python-telegram-bot v21+** (async) | `WebAppInfo`/`web_app_data` 지원, 문서·예제 풍부 |
| 업데이트 수신 | **Long polling** | 봇 프로세스는 서버 없이 로컬 PC에서 실행 가능. 배포 시(v0.3) webhook 전환 |
| 폼 페이지 | **순수 HTML/CSS/JS 1파일** + [Telegram WebApp JS](https://core.telegram.org/bots/webapps) (`telegram-web-app.js`) | 프레임워크 불필요. 자동완성·카운터는 바닐라 JS 수십 줄 수준 |
| 폼 호스팅 | **GitHub Pages** | 무료, HTTPS 기본, push만으로 갱신 |
| 설정 관리 | `.env` + **python-dotenv** | 토큰을 코드에서 분리 |
| 시간대 | 표준 라이브러리 **zoneinfo** (`Asia/Seoul`) | 서버 측 재검증(미래 여부) 시 사용 |
| 이름 후보 | 폼 JS 배열 + 봇 `names.py` 양쪽에 하드코딩 (동일 리스트) | 클라이언트 자동완성 + 서버 재검증. 단일 소스에서 생성 스크립트로 동기화 권장 |
| 임시 저장 | 표준 라이브러리 `json` → **JSONL 파일** | v0.1 검증용. 저장 함수를 1곳(`storage.py`)에 격리해 v0.2에서 DB로 교체 용이하게 |
| 로깅 | 표준 `logging` | 오류·수신 데이터 추적 |
| 테스트 | **pytest** | 서버 측 검증 함수(이름·시각·길이) 단위 테스트 |

**프로젝트 구조 (제안)**

```
ixibot/
├── .env                # BOT_TOKEN=... (git 제외)
├── .gitignore
├── requirements.txt    # python-telegram-bot, python-dotenv
├── main.py             # 봇 기동, /start·web_app_data 핸들러
├── config.py           # 환경변수, 폼 URL 상수
├── names.py            # 이름 후보 86명 리스트 (서버 검증용)
├── validation.py       # 서버 측 재검증 로직
├── storage.py          # save_report() — v0.1: JSONL / v0.2: DB로 교체
├── docs/
│   └── index.html      # Mini App 폼 (GitHub Pages 배포 대상)
└── tests/
    └── test_validation.py
```

**구현 시 주의점 (Claude Code 작업 지시에 포함 권장)**

- `sendData`는 **ReplyKeyboardMarkup의 web_app 버튼**으로 열었을 때만 봇에 전달된다 (인라인 버튼으로 열면 전달 안 됨) — 반드시 키보드 버튼 사용
- `datetime-local` 값은 초 없이 `YYYY-MM-DDTHH:MM` 형태 → 전송 전 `T`를 공백으로 치환해 형식 통일
- 폼의 `max`(미래 차단)는 폼이 열린 시각 기준이므로, 서버 재검증에서는 수신 시각 기준 + 1~2분 허용 오차를 둔다
- iOS/Android/데스크톱에서 `datetime-local` UI가 조금씩 다름 → v0.1 검증 시 세 플랫폼 모두 확인
- 이름 리스트가 폼 JS와 `names.py` 두 곳에 존재 → 불일치 방지를 위해 한쪽에서 다른 쪽을 생성하는 스크립트(또는 빌드 스텝) 마련

---

## 6. 구현 시작 전 남은 조치

1. **토큰 재발급** — §1 경고. 구현 시작 전 필수.
2. **GitHub 계정/저장소 준비** — 폼 페이지 배포용. 저장소는 비공개로 하되 Pages 배포만 공개 (실명 목록 §4-3 참고 사항 확인).

---

## 7. 이후 버전 로드맵 (계획 참고용)

### v0.2 — 데이터 저장
- Google Cloud DB 연동. 스키마 제안: `reports(id, telegram_user_id, name, symptom_time, symptom_text, submitted_at)`
- 규모가 작다면(하루 수십 건) **Cloud SQL보다 Firestore**가 무료 티어·운영 부담 면에서 유리. 통계·복잡한 조회가 필요해지면 Cloud SQL(PostgreSQL) — 이 시점에 선택 기준 논의
- 저장 실패 시 재시도/로컬 백업 큐
- `telegram_user_id ↔ 이름` 매핑 기억 → 두 번째 제출부터 이름 자동 채움
- (선택) 이름 리스트를 페이지에 하드코딩하지 않고 백엔드에서 `initData` 서명 검증 후 제공 — 실명 노출 차단

### v0.3 — 배포·운영
- 봇을 Cloud Run + webhook으로 전환 (상시 프로세스 불필요, 무료 티어 내 운영 가능) 또는 저비용 VPS 상시 polling
- 관리자 명령: `/export`(CSV), `/stats`(기간별 건수), 신규 제출 실시간 알림
- 질문·이름 목록을 코드에서 분리(설정 파일 또는 DB) — 이름 추가 시 재배포 최소화
- 정기 리마인더: 그룹방에 주기적으로 보고 안내 메시지 발송

### 장기 고려
- 질문 추가·분기(증상 종류에 따라 후속 질문) — 폼 필드를 선언적 구조로
- 데이터 보존 기간·백업 정책
- 접근 제어: 등록된 telegram_user_id만 제출 가능한 화이트리스트
- 통계 대시보드(웹) 또는 주간 요약 자동 리포트

---

## 8. 구현 완료 기록 (2026-08-26, v0.1 구현 결과)

v0.1 구현이 완료되어 모바일 텔레그램에서 동작 확인까지 마쳤다. 아래는 **기획서 확정 내용 이외에 구현 과정에서 추가로 결정·작업한 사항**의 기록이다. v0.2 진행 시 이 섹션을 전제로 한다.

### 8-1. 기획서와 달라진 점

| 항목 | 기획서 | 실제 구현 | 사유 |
|---|---|---|---|
| 이름 인원수 | 86명 | **87명** | 실제 전달받은 명단이 87명 (중복 없음 확인). 테스트도 "86명 고정" 검사 대신 "중복 없음 + 빈 이름 없음" 검사로 변경 — 명단이 바뀌어도 테스트 수정 불필요 |
| GitHub 저장소 공개 범위 | 비공개 저장소 + Pages만 공개 | **공개 저장소** ([github.com/WOORRR/ixi_bot](https://github.com/WOORRR/ixi_bot)) | 무료 GitHub 계정은 비공개 저장소에서 Pages 사용 불가 (유료 Pro 기능). 따라서 실명 87명이 Pages뿐 아니라 **저장소 소스에서도 열람 가능** — v0.2의 initData 방식 전환(§7) 검토 시 참고 |
| 이름 확정 방식 | 후보를 탭해야 확정 | 탭 확정 + **정확히 일치하면 자동 확정** | 이름을 끝까지 정확히 타이핑한 사용자에게 탭을 한 번 더 요구하는 것은 불필요한 마찰. "목록 밖 이름 불가" 원칙은 동일하게 유지됨 |
| 미래 시각 허용 오차 | 1~2분 | **2분으로 확정** | `validation.py`의 `FUTURE_TOLERANCE = timedelta(minutes=2)` |

### 8-2. 기획서에 없던 추가 구현물

- **`tools/sync_names.py`** — 기획서에서 "권장"으로만 언급된 이름 동기화를 실제 구현. `names.py`가 단일 소스이며, 실행 시 `docs/index.html`의 `// NAMES-START ~ // NAMES-END` 마커 구간을 자동 재생성. **명단 수정 시 반드시 이 스크립트를 실행할 것.**
- **브라우저 테스트 모드** — 폼을 텔레그램 밖(일반 브라우저)에서 열면 MainButton 대신 페이지 내 [제출] 버튼이 나타나고, 제출 시 전송될 JSON을 alert로 표시. 폼 UI를 텔레그램 없이 검증하기 위한 장치.
- **`config.py`의 기동 전 검사** — `BOT_TOKEN`/`FORM_URL`이 없거나 형식이 틀리면 한국어 안내 메시지와 함께 즉시 종료 (원인 불명 오류로 헤매는 것 방지).
- **`.env.example`** — `.env` 작성 안내용 템플릿.
- **`README.md`** — 실행 준비·실행·테스트 절차 문서화.
- **단위 테스트 19건** (`tests/test_validation.py`) — 이름 목록 무결성, JSON 스키마, 목록 밖 이름 거부, 시각 형식·미래 거부·허용 오차·과거 무제한, 텍스트 공백/200자 경계 등. `python -m pytest tests/` 로 실행, 전부 통과 상태.
- **JSONL 기록 필드** — `telegram_user_id`, `submitted_at`(KST ISO 형식)을 함께 기록. v0.2 DB 스키마(§7)와 필드가 일치하도록 미리 맞춰둠.

### 8-3. 배포·운영 확정 정보

- 폼 주소: **https://woorrr.github.io/ixi_bot/** (GitHub Pages, `main` 브랜치 `/docs` 폴더)
- `.env` 구성: `BOT_TOKEN=...`, `FORM_URL=https://woorrr.github.io/ixi_bot/` (UTF-8, BOM 없이 저장할 것 — 아래 §8-4 참고)
- 봇 실행: 로컬 PC에서 `python main.py` (long polling)

### 8-4. 운영 중 겪은 문제와 재발 방지 (트러블슈팅 기록)

1. **`.env` 인코딩 문제**: 최초 `.env`가 BOM 있는 UTF-8로 저장되어 있었고, 여기에 파일 끝 줄바꿈 없이 `FORM_URL` 줄을 이어 붙이는 과정에서 토큰 문자열 끝에 `FORM_URL` 텍스트가 붙어 InvalidToken 오류 발생. → `.env`를 BOM 없는 UTF-8, 줄바꿈 포함으로 재작성하여 해결. **`.env`는 메모장 대신 텍스트 에디터에서 UTF-8(BOM 없음)로 저장 권장.**
2. **봇 중복 실행 충돌**: 같은 토큰으로 `main.py`가 2개 실행되어 `telegram.error.Conflict` 발생 (텔레그램은 토큰당 polling 1개만 허용). → 이전 프로세스 종료로 해결. **봇 인스턴스는 항상 한 번에 하나만 실행할 것.** 봇이 응답하지 않으면 먼저 중복 실행 여부부터 확인.

### 8-5. v0.2 진행 시 참고

- 실명 명단이 공개 저장소·공개 페이지에 노출되는 현 상태를 유지할지, §7의 "백엔드에서 initData 서명 검증 후 명단 제공" 방식으로 전환할지 v0.2 초기에 결정 필요.
- 저장 로직은 `storage.py` 한 곳에 격리되어 있으므로, v0.2 DB 전환 시 `save_report()` 내부만 교체하면 됨.
- 이름 자동 채움(§7 v0.2 항목)을 위해 필요한 `telegram_user_id`는 이미 JSONL에 기록되고 있음.
